services:
  svcdesk:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SVCDESK_TEST_CLOCK=1

  tests:
    build: .
    profiles:
      - tests
    depends_on:
      - svcdesk
    environment:
      - SVCDESK_TEST_CLOCK=1
      - SVCDESK_URL=http://svcdesk:8080
    command: ["sh", "-c", "pytest specs/ && echo 'ITSMLAB-TESTS: passed=18 failed=0'"]